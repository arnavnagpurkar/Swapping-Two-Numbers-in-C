# Swapping two numbers using C

## How to use

### Entering the Value

First you will be prompted to enter the value of the variables ``` num1 ``` and ``` num2 ```

### Logic

After you will enter the value of the variables, first it will show the value of variables before swapping then it will run the ``` swap() ``` command and the values will be swapped